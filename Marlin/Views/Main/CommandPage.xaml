<Page x:Class="Marlin.Views.Main.CommandPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:vm="clr-namespace:Marlin.ViewModels.Main"
      xmlns:local="clr-namespace:Marlin.Views.Main"
      mc:Ignorable="d" 
      d:DesignHeight="450" 
      d:DesignWidth="800"
      Title="CommandPage"
      Foreground="{Binding Path=FontColor}">
    <Page.DataContext>
        <vm:CommandPageViewModel/>
    </Page.DataContext>
    <Grid Background="{Binding Path=ExternalBackgroundColor,UpdateSourceTrigger=PropertyChanged}">
        <Grid>
            <Grid.Background>
                <ImageBrush ImageSource="{Binding Path=BackgraundImagePath,UpdateSourceTrigger=PropertyChanged}"
                            Stretch="{Binding Path=Stretch,UpdateSourceTrigger=PropertyChanged}"
                            TileMode="{Binding Path=TileMode,UpdateSourceTrigger=PropertyChanged}"
                            ViewportUnits="{Binding Path=ViewportUnits,UpdateSourceTrigger=PropertyChanged}"
                            Viewport="{Binding Path=ImageViewport,UpdateSourceTrigger=PropertyChanged}"/>
            </Grid.Background>

            <Grid.RowDefinitions>
                <RowDefinition Height="50px"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="auto"/>
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0"
                       Background="Transparent"
                       HorizontalAlignment="Center"
                       VerticalAlignment="Center"
                       FontSize="30"
                       Margin="5"
                       FontWeight="Bold"
                       Padding="30,0"
                       Text="{Binding Path=Title}"/>

            <Border Grid.Row="1"
                    Margin="50,0,50,5"
                    CornerRadius="25"
                    Background="{Binding Path=InternalBackgroundColor,UpdateSourceTrigger=PropertyChanged}"
                    BorderBrush="{Binding Path=PageColor,UpdateSourceTrigger=PropertyChanged}"
                    BorderThickness="2">
                <ScrollViewer VerticalScrollBarVisibility="Hidden">
                    <Grid Margin="10">
                        <Grid.RowDefinitions>
                            <RowDefinition Height ="auto"/>
                            <RowDefinition Height ="auto"/>
                            <RowDefinition Height ="auto"/>
                        </Grid.RowDefinitions>

                        <Grid Grid.Row="0">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <TextBlock Grid.Column="0"
                                       Margin="10,0"
                                       FontSize="20"
                                       VerticalAlignment="Top"
                                       FontWeight="Bold"
                                       Foreground="{Binding Path=PageColor,UpdateSourceTrigger=PropertyChanged}"
                                       Text="Основное:"/>

                            <Grid Grid.Column="1"
                                  Margin="0,4,0,0">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="auto"/>
                                    <ColumnDefinition Width="auto"/>
                                </Grid.ColumnDefinitions>
                                
                                <TextBlock Grid.Column="0"  
                                           Margin="0,0,10,0"
                                           FontSize="15"
                                           VerticalAlignment="Center"
                                           FontWeight="Bold"
                                           Foreground="{Binding Path=FontColor,UpdateSourceTrigger=PropertyChanged}"
                                           Text="Название команды"/>

                                <TextBox Grid.Column="1"
                                         MinWidth="100"
                                         FontSize="15"
                                         Padding="10,0"
                                         HorizontalContentAlignment="Center"
                                         VerticalAlignment="Center"
                                         Text="{Binding Path=CommandTitle,UpdateSourceTrigger=PropertyChanged}"/>
                            </Grid>
                        </Grid>

                        <Grid Grid.Row="1"
                              Margin="0,10,0,0">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="auto"/>
                                <ColumnDefinition Width="auto"/>
                            </Grid.ColumnDefinitions>

                            <TextBlock Grid.Column="0"
                                       Margin="10,0"
                                       FontSize="20"
                                       VerticalAlignment="Top"
                                       FontWeight="Bold"
                                       Foreground="{Binding Path=PageColor,UpdateSourceTrigger=PropertyChanged}"
                                       Text="Действие:"/>

                            <Grid Grid.Column="1"
                                  Margin="0,1,0,0">
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="*"/>
                                    <RowDefinition Height="{Binding Path=LengthOwnActions,UpdateSourceTrigger=PropertyChanged}"/>
                                    <RowDefinition Height="{Binding Path=LengthEmbeddedActions,UpdateSourceTrigger=PropertyChanged}"/>
                                </Grid.RowDefinitions>

                                <Grid Grid.Row="0">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="auto"/>
                                        <ColumnDefinition Width="auto"/>
                                    </Grid.ColumnDefinitions>

                                    <TextBlock Grid.Column="0"  
                                               Margin="0,0,10,0"
                                               FontSize="15"
                                               VerticalAlignment="Center"
                                               FontWeight="Bold"
                                               Foreground="{Binding Path=FontColor,UpdateSourceTrigger=PropertyChanged}"
                                               Text="Выберите действие"/> <!--создать свое действие\использовать встроенные методы--> 

                                    <ComboBox Grid.Column="1"
                                              FontSize="15"
                                              SelectedValue="{Binding Path=SelectedAction,UpdateSourceTrigger=PropertyChanged}"
                                              ItemsSource="{Binding Path=Actions,UpdateSourceTrigger=PropertyChanged}"/>
                                </Grid>
                                <!--Создание своего действия-->
                                <Grid Grid.Row="1">
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="auto"/>
                                        <RowDefinition Height="{Binding Path=LengthReadyCmdCommand,UpdateSourceTrigger=PropertyChanged}"/>
                                        <RowDefinition Height="{Binding Path=LengthCommandConstructor,UpdateSourceTrigger=PropertyChanged}"/>
                                    </Grid.RowDefinitions>

                                    <!--Выбор моздавать или нет команду-->
                                    <Grid Grid.Row="0"
                                          Margin="0,10">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="auto"/>
                                            <ColumnDefinition Width="auto"/>
                                        </Grid.ColumnDefinitions>

                                        <TextBlock Grid.Column="0" 
                                                       Margin="0,0,10,0"
                                                       FontSize="15"
                                                       VerticalAlignment="Center"
                                                       FontWeight="Bold"
                                                       Foreground="{Binding Path=FontColor,UpdateSourceTrigger=PropertyChanged}"
                                                       Text="Готовая команда cmd"/>

                                        <CheckBox Grid.Column="1"
                                                      IsChecked="{Binding Path=IsReadyCmdCommand}"
                                                      VerticalAlignment="Center"/>
                                    </Grid>
                                    <!--Готовая команда cmd-->
                                    <Grid Grid.Row="1"
                                          Margin="0,4,0,10">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="auto"/>
                                            <ColumnDefinition Width="auto"/>
                                        </Grid.ColumnDefinitions>

                                        <TextBlock Grid.Column="0"  
                                                   Margin="0,0,10,0"
                                                   FontSize="15"
                                                   VerticalAlignment="Center"
                                                   FontWeight="Bold"
                                                   Foreground="{Binding Path=FontColor,UpdateSourceTrigger=PropertyChanged}"
                                                   Text="Введите команду cmd"/>

                                        <TextBox Grid.Column="1"
                                                 MinWidth="100"
                                                 Margin="0,0,10,0"
                                                 FontSize="15"
                                                 Padding="10,0"
                                                 HorizontalContentAlignment="Center"
                                                 VerticalAlignment="Center"
                                                 Text=""/>
                                    </Grid>
                                    <!--Конструктор команд-->
                                    <Grid Grid.Row="2">
                                        <Grid.RowDefinitions>
                                            <RowDefinition Height="auto"/>
                                            <RowDefinition Height="{Binding Path=LengthChoseObject}"/>
                                            <RowDefinition Height="{Binding Path=LengthInputUrl}"/>
                                            <RowDefinition Height="{Binding Path=LengthChoseApp}"/>
                                        </Grid.RowDefinitions>

                                        <Grid Grid.Row="0">
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="auto"/>
                                                <ColumnDefinition Width="auto"/>
                                            </Grid.ColumnDefinitions>
                                            
                                            <TextBlock Grid.Column="0" 
                                                       FontSize="15"
                                                       Margin="0,0,10,0"
                                                       VerticalAlignment="Center"
                                                       Foreground="{Binding Path=FontColor,UpdateSourceTrigger=PropertyChanged}"
                                                       Text="Выберите тип объекта"/>

                                            <ComboBox Grid.Column="1"
                                                      FontSize="15"
                                                      SelectedValue="{Binding Path=SelectedObject,UpdateSourceTrigger=PropertyChanged}"
                                                      ItemsSource="{Binding Path=Objects,UpdateSourceTrigger=PropertyChanged}"/>
                                        </Grid>

                                        <Grid Grid.Row="1"
                                              Margin="0,10,0,5">
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="auto"/>
                                                <ColumnDefinition Width="auto"/>
                                            </Grid.ColumnDefinitions>

                                            <Button Grid.Column="0" 
                                                    FontSize="15"
                                                    Margin="0,0,10,0"
                                                    VerticalAlignment="Center"
                                                    Foreground="{Binding Path=ExternalBackgroundColor,UpdateSourceTrigger=PropertyChanged}"
                                                    Content="Выберите объект"/>

                                            <TextBox Grid.Column="1"
                                                     MinWidth="100"
                                                     FontSize="15"
                                                     Padding="10,0"
                                                     IsReadOnly="True"
                                                     HorizontalContentAlignment="Center"
                                                     VerticalAlignment="Center"
                                                     Text=""/>
                                        </Grid>

                                        <Grid Grid.Row="2"
                                              Margin="0,10">
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="auto"/>
                                                <ColumnDefinition Width="auto"/>
                                            </Grid.ColumnDefinitions>

                                            <TextBlock Grid.Column="0" 
                                                       FontSize="15"
                                                       Margin="0,0,10,0"
                                                       VerticalAlignment="Center"
                                                       Foreground="{Binding Path=FontColor,UpdateSourceTrigger=PropertyChanged}"
                                                       Text="Введите URL"/>

                                            <TextBox Grid.Column="1"
                                                     MinWidth="100"
                                                     FontSize="15"
                                                     Padding="10,0"
                                                     IsReadOnly="True"
                                                     HorizontalContentAlignment="Center"
                                                     VerticalAlignment="Center"
                                                     Text=""/>
                                        </Grid>

                                        <Grid Grid.Row="3"
                                              Margin="0,5,0,10">
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="auto"/>
                                                <ColumnDefinition Width="auto"/>
                                            </Grid.ColumnDefinitions>

                                            <Button Grid.Column="0" 
                                                    FontSize="15"
                                                    Margin="0,0,10,0"
                                                    VerticalAlignment="Center"
                                                    Foreground="{Binding Path=ExternalBackgroundColor,UpdateSourceTrigger=PropertyChanged}"
                                                    Content="Выберите программу для запуска"/>

                                            <TextBox Grid.Column="1"
                                                     MinWidth="100"
                                                     FontSize="15"
                                                     Padding="10,0"
                                                     IsReadOnly="True"
                                                     HorizontalContentAlignment="Center"
                                                     VerticalAlignment="Center"
                                                     Text=""/>
                                        </Grid>
                                    </Grid>
                                </Grid>
                                
                                <!--Использование встроенных методов-->
                                <Grid Grid.Row="2">
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="auto"/>
                                        <RowDefinition Height="auto"/>
                                    </Grid.RowDefinitions>

                                    <Grid Grid.Row="0"
                                          Margin="0,6,0,10">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="auto"/>
                                            <ColumnDefinition Width="auto"/>
                                        </Grid.ColumnDefinitions>

                                        <TextBlock Grid.Column="0"  
                                                   FontSize="15"
                                                   Margin="0,0,10,0"
                                                   VerticalAlignment="Center"
                                                   FontWeight="Bold"
                                                   Foreground="{Binding Path=FontColor,UpdateSourceTrigger=PropertyChanged}"
                                                   Text="Выберите метод"/>

                                        <ComboBox Grid.Column="1"
                                                      FontSize="15"
                                                      SelectedValue="{Binding Path=SelectedEmbeddedAction,UpdateSourceTrigger=PropertyChanged}"
                                                      ItemsSource="{Binding Path=EmbeddedActions,UpdateSourceTrigger=PropertyChanged}"/>
                                    </Grid>

                                    <Grid Grid.Row="1">
                                        <Grid.RowDefinitions>
                                            <RowDefinition Height="{Binding Path=LengthTextToSpeech}"/>
                                            <RowDefinition Height="{Binding Path=LengthPressingKeys}"/>
                                            <RowDefinition Height="{Binding Path=LengthMovingCursor}"/>
                                        </Grid.RowDefinitions>

                                        <!--Озвучивание файла-->
                                        <Grid Grid.Row="0">
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="auto"/>
                                                <ColumnDefinition Width="auto"/>
                                            </Grid.ColumnDefinitions>

                                            <Button Grid.Column="0" 
                                                    FontSize="15"
                                                    Margin="0,0,10,0"
                                                    VerticalAlignment="Center"
                                                    Foreground="{Binding Path=ExternalBackgroundColor,UpdateSourceTrigger=PropertyChanged}"
                                                    Content="Выберите фаил"/>

                                            <TextBox Grid.Column="1"
                                                     MinWidth="100"
                                                     FontSize="15"
                                                     Padding="10,0"
                                                     IsReadOnly="True"
                                                     HorizontalContentAlignment="Center"
                                                     VerticalAlignment="Center"
                                                     Text=""/>
                                        </Grid>
                                        <!--Нажатие клавиш-->
                                        <Grid Grid.Row="1">
                                            <Grid.RowDefinitions>
                                                <RowDefinition Height="auto"/>
                                                <RowDefinition Height="{Binding Path=LengthSymbolCode}"/>
                                                <RowDefinition Height="{Binding Path=LengthMultiSymbol}"/>
                                            </Grid.RowDefinitions>

                                            <Grid Grid.Row="0"
                                                  Margin="0,0,0,10">
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="auto"/>
                                                    <ColumnDefinition Width="auto"/>
                                                </Grid.ColumnDefinitions>

                                                <TextBlock Grid.Column="0"
                                                           FontSize="15"
                                                           Margin="0,0,10,0"
                                                           VerticalAlignment="Center"
                                                           FontWeight="Bold"
                                                           Foreground="{Binding Path=FontColor,UpdateSourceTrigger=PropertyChanged}"
                                                           Text="Ввод множества символов"/>

                                                <CheckBox Grid.Column="1"
                                                          IsChecked="{Binding Path=IsMultiSymbol}"
                                                          VerticalAlignment="Center"/>
                                            </Grid>
                                            
                                            <Grid Grid.Row="1"
                                                  Margin="0,0,0,10">
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="auto"/>
                                                    <ColumnDefinition Width="auto"/>
                                                </Grid.ColumnDefinitions>

                                                <TextBlock Grid.Column="0" 
                                                           FontSize="15"
                                                           Margin="0,5,10,0"
                                                           VerticalAlignment="Center"
                                                           FontWeight="Bold"
                                                           Foreground="{Binding Path=FontColor,UpdateSourceTrigger=PropertyChanged}"
                                                           Text="Введите код клавиши"/>

                                                <TextBox Grid.Column="1"
                                                         MinWidth="100"
                                                         FontSize="15"
                                                         Padding="10,0"
                                                         HorizontalContentAlignment="Center"
                                                         VerticalAlignment="Center"
                                                         Text=""/>
                                            </Grid>
                                            
                                            <Grid Grid.Row="2"
                                                  Margin="0,0,0,10">
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="auto"/>
                                                    <ColumnDefinition Width="auto"/>
                                                </Grid.ColumnDefinitions>

                                                <TextBlock Grid.Column="0"
                                                           FontSize="15"
                                                           Margin="0,5,10,0"
                                                           VerticalAlignment="Center"
                                                           FontWeight="Bold"
                                                           Foreground="{Binding Path=FontColor,UpdateSourceTrigger=PropertyChanged}"
                                                           Text="Введите текст"/>

                                                <TextBox Grid.Column="1"
                                                         MinWidth="100"
                                                         FontSize="15"
                                                         Padding="10,0"
                                                         HorizontalContentAlignment="Center"
                                                         VerticalAlignment="Center"
                                                         Text=""/>
                                            </Grid>
                                        </Grid>
                                        <!--Переместить курсор-->
                                        <Grid Grid.Row="2"
                                              Margin="0,0,0,5">
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="auto"/>
                                                <ColumnDefinition Width="auto"/>
                                                <ColumnDefinition Width="auto"/>
                                                <ColumnDefinition Width="auto"/>
                                                <ColumnDefinition Width="auto"/>
                                            </Grid.ColumnDefinitions>
                                            <TextBlock Grid.Column="0"  
                                                       FontSize="15"
                                                        Margin="0,0,10,0"
                                                       VerticalAlignment="Center"
                                                       FontWeight="Bold"
                                                       Foreground="{Binding Path=FontColor,UpdateSourceTrigger=PropertyChanged}"
                                                       Text="Введите координаты"/>

                                            <TextBlock Grid.Column="1"  
                                                       FontSize="15"
                                                       Margin="0,0,5,0"
                                                       VerticalAlignment="Center"
                                                       FontWeight="Bold"
                                                       Foreground="{Binding Path=FontColor,UpdateSourceTrigger=PropertyChanged}"
                                                       Text="X:"/>

                                            <TextBox Grid.Column="2"
                                                     MinWidth="50"
                                                     Margin="0,0,10,0"
                                                     FontSize="15"
                                                     Padding="10,0"
                                                     HorizontalContentAlignment="Center"
                                                     VerticalAlignment="Center"
                                                     Text=""/>

                                            <TextBlock Grid.Column="3"  
                                                       FontSize="15"
                                                       Margin="0,0,5,0"
                                                       VerticalAlignment="Center"
                                                       FontWeight="Bold"
                                                       Foreground="{Binding Path=FontColor,UpdateSourceTrigger=PropertyChanged}"
                                                       Text="Y:"/>

                                            <TextBox Grid.Column="4"
                                                     MinWidth="50"
                                                     FontSize="15"
                                                     Padding="10,0"
                                                     HorizontalContentAlignment="Center"
                                                     VerticalAlignment="Center"
                                                     Text=""/>
                                        </Grid>
                                    </Grid>
                                </Grid>
                            </Grid>
                        </Grid>

                        <Grid Grid.Row="2"
                              Margin="0,10">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="auto"/>
                                <ColumnDefinition Width="auto"/>
                            </Grid.ColumnDefinitions>

                            <TextBlock Grid.Column="0"
                                       FontSize="20"
                                       VerticalAlignment="Top"
                                       FontWeight="Bold"
                                       Foreground="{Binding Path=PageColor,UpdateSourceTrigger=PropertyChanged}"
                                       Text="Активация:"/>

                            <Grid Grid.Column="1">
                                <Grid.RowDefinitions>
                                    <RowDefinition Height ="auto"/>
                                    <RowDefinition Height ="auto"/>
                                </Grid.RowDefinitions>

                                <Grid Grid.Row="0"
                                      Margin="10,5">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="auto"/>
                                        <ColumnDefinition Width="auto"/>
                                    </Grid.ColumnDefinitions>
                                    
                                    <TextBlock Grid.Column="0"  
                                               FontSize="15"
                                               Margin="0,0,10,0"
                                               FontWeight="Bold"
                                               Foreground="{Binding Path=FontColor,UpdateSourceTrigger=PropertyChanged}"
                                               Text="Запрашивать пароль перед запуском"/>
                                    
                                    <CheckBox Grid.Column="1"
                                              IsChecked="False"/>
                                </Grid>

                                <Grid Grid.Row="1">
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="auto"/>
                                        <RowDefinition Height="auto"/>
                                    </Grid.RowDefinitions>

                                    <StackPanel Grid.Row="0"
                                                Margin="10">
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="auto"/>
                                                <ColumnDefinition Width="auto"/>
                                            </Grid.ColumnDefinitions>

                                            <ComboBox Grid.Column="0"
                                                      FontSize="15"
                                                      Margin="0,0,10,0"
                                                      SelectedValue="{Binding Path=SelectedTrigger,UpdateSourceTrigger=PropertyChanged}"
                                                      ItemsSource="{Binding Path=Triggers,UpdateSourceTrigger=PropertyChanged}"
                                                      VerticalAlignment="Top"
                                                      MinWidth="50"/>

                                            <Grid Grid.Column="1">
                                                <Grid.RowDefinitions>
                                                    <RowDefinition Height="{Binding Path=LengthTextTrigger}"/>
                                                    <RowDefinition Height="{Binding Path=LengthAppTrigger}"/>
                                                </Grid.RowDefinitions>

                                                <TextBox Grid.Row="0"
                                                         Margin="0,8,0,0"
                                                         MinWidth="100"
                                                         FontSize="15"
                                                         Padding="10,0"
                                                         HorizontalContentAlignment="Center"
                                                         Text="Фраза"/>

                                                <Grid Grid.Row="2">
                                                    <Grid.ColumnDefinitions>
                                                        <ColumnDefinition Width="auto"/>
                                                        <ColumnDefinition Width="auto"/>
                                                    </Grid.ColumnDefinitions>

                                                    <Button Grid.Column="0" 
                                                            FontSize="15"
                                                            Margin="0,0,10,0"
                                                            VerticalAlignment="Center"
                                                            Foreground="{Binding Path=ExternalBackgroundColor,UpdateSourceTrigger=PropertyChanged}"
                                                            Content="Выберите программу"/>

                                                    <TextBox Grid.Column="1"
                                                             MinWidth="100"
                                                             FontSize="15"
                                                             Padding="10,0"
                                                             IsReadOnly="True"
                                                             HorizontalContentAlignment="Center"
                                                             VerticalAlignment="Center"
                                                             Text="Программа 1"/>
                                                </Grid>
                                            </Grid>
                                        </Grid>

                                    </StackPanel>

                                    <Button Grid.Row="1"
                                            FontSize="15"
                                            Width="auto"
                                            HorizontalAlignment="Center"
                                            Foreground="{Binding Path=ExternalBackgroundColor,UpdateSourceTrigger=PropertyChanged}"
                                            Content="Добавить триггер"/>
                                </Grid>
                            </Grid>
                        </Grid>
                    </Grid>
                </ScrollViewer>
            </Border>

            <Grid Grid.Row="2">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="auto"/>
                    <ColumnDefinition Width="auto"/>
                </Grid.ColumnDefinitions>

                <Button Grid.Column="0"
                        Content="Назад"
                        Margin="50,10,5,10"
                        FontSize="15"
                        FontWeight="Bold"
                        Command="{Binding Path=ToMainCommand}"
                        BorderThickness="0"
                        BorderBrush="Transparent"
                        Foreground="{Binding Path=ExternalBackgroundColor,UpdateSourceTrigger=PropertyChanged}"
                        Background="{Binding Path=PageColor,UpdateSourceTrigger=PropertyChanged}"/>

                <Button Grid.Column="1"
                        Content="{Binding Path=ButtonContent}"
                        FontSize="15"
                        FontWeight="Bold"
                        Margin="5,10,50,10" 
                        BorderThickness="0"
                        BorderBrush="Transparent"
                        Foreground="{Binding Path=ExternalBackgroundColor,UpdateSourceTrigger=PropertyChanged}"
                        Background="{Binding Path=PageColor,UpdateSourceTrigger=PropertyChanged}"
                        Command="{Binding Path=ButtonActionCommand}"/>
            </Grid>
        </Grid>
    </Grid>
</Page>
